{
  "name": "vite-starter",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "dev": "node scripts/scanFolders.js && vite",
    "build": "node scripts/scanFolders.js && vite build",
    "preview": "vite preview",
    "scan": "node scripts/scanFolders.js"
  },
  "devDependencies": {
    "vite": "^5.4.2"
  }
}